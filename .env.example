# Copy this file to .env for local docker-compose usage.

# Neo4j credentials used by both the database container and the backend API.
NEO4J_AUTH=neo4j/expand-local-dev-password

# Bootstrap configuration for the initial platform admin.
ACCESS_BOOTSTRAP_MODE=development
ACCESS_BOOTSTRAP_ADMIN_PASSWORD=change-this-local-admin-password
